:host {
	position: absolute;
	width: 100%;
	z-index: 5;

	top: 1rem;
}

.gallery-row {
	height: 10em;
	overflow-x: auto;
	white-space: nowrap;

	img {
		height: 100%;
		border: 1px solid rgba(0, 0, 0, .5);
		cursor: pointer;

		&:hover {
			border-style: dashed;
		}
	}
}
