.container.card.p-a-1
	.card-block
		h4.card-title draw image

		.alert.alert-danger('*ngIf'='layerService.isAtLimit') you cannot add more images...

		form.form-inline('[hidden]'='layerService.isAtLimit')
			.row
				.col-md-9
					canvas(
						'#canvasRef'='',
						'[width]'='canvasRef.offsetWidth',
						'[height]'='canvasRef.offsetHeight',
						'(mousedown)'='init($event)',
						'(mouseleave)'='drawing = false',
						'(mouseup)'='drawing = false',
						'(mousemove)'='draw($event)',
						'(dragstart)'='$event.preventDefault()'
					)
				.col-md-3
					.form-group
						label color:
						input(type='color', '[(ngModel)]'='color', name='color')
					.form-group
						label stroke width:
						input(type='range', min=1, max=100, '[(ngModel)]'='strokeWidth', name='strokeWidth')


			.row.m-y-1
				.col-md-6
					button.btn.btn-primary.btn-block(type='button', '(click)'='add()') add image
				.col-md-6
					button.btn.btn-danger.btn-block(type='button', '(click)'='clear()') clear canvas

		.row
			a.btn.btn-warning.btn-block([routerLink]="['', { outlets: { aux: null } }]") close drawer

