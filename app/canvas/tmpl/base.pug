.base-wrapper

	img.fn-deactivate(
		'#image'='',
		'[src]'='base.src',
		'(dragstart)'='$event.preventDefault()',
		'(click)'='layerService.active = null'
	)

	.base-canvas(
		'[style.height.px]'='base.canvasHeight * coefficient',
		'[style.width.px]'='base.canvasWidth * coefficient',
		'[style.top.px]'='base.canvasY * coefficient',
		'[style.left.px]'='base.canvasX * coefficient',
		'(click)'='$event.target === $event.currentTarget ? (layerService.active = null) : null'
	)
		ng-content
