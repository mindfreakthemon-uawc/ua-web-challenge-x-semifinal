:host {
	display: block;
}

.base-wrapper {
	position: relative;
	user-select: none;
	outline: 1px solid #000;
	box-shadow: 1px 1px 2em #000;

	img {
		width: 100%;
		height: 100%;
	}
}

.base-canvas {
	position: absolute;
	outline: 1px dotted #000;
	overflow: hidden;
}
