.base-wrapper {
	position: relative;
	outline: 1px solid #000;
	user-select: none;
	box-shadow: 1px 1px 30px #000;

	img {
		width: 100%;
	}
}

.base-canvas {
	position: absolute;
	outline: 1px dotted #000;
	overflow: hidden;
}
